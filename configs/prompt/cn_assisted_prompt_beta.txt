输入语句：[TEXT]

你现在需要对于输入语句进行语法纠错，让我们逐步解决这个问题。

1.现在有一个较为准确的检错模块对于输入语句进行了检测，每个错误的区间用一个[MASK]替换掉；如果没有检测到错误，句子不会被添加[MASK]。处理后的语句如下：
检错后语句：[MASKED_TEXT]

2.你需要基于检错后语句，和原始的输入语句，对于[MASK]的位置进行填充，以形成一个语法正确的句子。
注意：
（1）如果没有检错到错误，检错后语句中不会出现[MASK]，如果你也认为输入语句没有错误，请将输入语句作为改正后语句直接输出。
（2）如果你认为在某些没有[MASK]的位置也存在语法错误，可以一并修改。
（3）检错模块可能不够准确，[MASK]位置可能本来没有语法错误，如果你认为检错模块有这种误报时，你可以把[MASK]位置的文本替换回来。
改正后语句：